services:
  - type: web
    name: quizzplus-backend
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MONGO_URL
        sync: false
        # ⚠️ IMPORTANT: Configurer cette variable avec ton URL MongoDB Atlas dans le dashboard Render
      - key: DB_NAME
        value: quizzplus
      - key: CORS_ORIGINS
        value: https://frontend-rho-henna.vercel.app,https://frontend-gfn1pzz0b-abdou-aziz-diops-projects.vercel.app,http://localhost:3000
      - key: PYTHON_VERSION
        value: 3.11.0
